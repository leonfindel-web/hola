---
import Base from '../../../layouts/Base.astro';
import WorkBody from '../../../components/pages/WorkBody.astro';
import { t } from '../../../lib/i18n';

const lang = 'es' as const;
const dict = t(lang);
---

<Base lang={lang} altPath="/work" title={dict.work.title} description={dict.work.intro}>
  <WorkBody lang={lang} />
</Base>
